require_relative 'spec_helper'

module TeeDub module FeatureFlags
 
  #describe DerivedFlags do
    #describe '#on?' do
      #it 'returns false for unknown flags' do
        #derived_flags = DerivedFlags.new( {} )
        #derived_flags.on?( :foo ).should be_false
      #end

      #it 'returns true for a flag which is on by default' do
        #derived_flags = DerivedFlags.new( } )

      #end
    #end
  #end
end end
